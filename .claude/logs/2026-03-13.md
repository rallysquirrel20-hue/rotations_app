# Session Log - 2026-03-13

## 1. Live breadth overlay fix
Sidebar BO%/Br% values were stale (sourced from cached parquet). Fixed `get_basket_breadth()` to overlay live-computed values using `_tally_breadth()` helper with a batch read of `signals_500.parquet`. Single read instead of N reads per basket.

## 2. Basket equity curve signals
Added columns to sidebar basket rows: LT (BO/BD), ST (UP/DN), MR (BTFD/STFR), Chg (% change), and Cor% (correlation). Live overlay uses `live_basket_signals_500.parquet` OHLC + cached pivots for incremental signal computation.

## 3. Basket sorting and filtering
Added sortable/filterable column headers for basket sections (Themes/Sectors/Industries). Supports sort by all columns, filter dropdowns for LT/ST/MR with same UX as the ticker list.

## 4. UI improvements
- Sidebar widened 360 -> 460px.
- All columns standardized to 40px centered.
- Correlation uses chart's grey (#586e75).
- Active row overrides correlation color to white.

## 5. BTFD/STFR signal fix (both repos)
Entry logic now requires previous bar's trend to match (`prev_trend==False` for BTFD, `prev_trend==True` for STFR). Entry prices use gap-fill logic (open vs target). Version bumped.

## 6. BTFD/STFR chart arrows
`TVChart.tsx` now shows entry arrows for BTFD (green up arrow) and STFR (pink down arrow).

## 7. rotations_signals cleanup
Group B report-only cells extracted to `rotations_old_outputs.py`.
